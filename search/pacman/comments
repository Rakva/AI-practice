from myagent.py

        # directions = game.get_possible_pacman_dirs(False) # საპირისპირო მიმართულებაც ითვლება True
        #ახლა გვინდა რომ გავიგოთ ყველა ბონუსის ლოკაცია და ყველა მტაცებლის/მსხვერპლის ლოკაცია
        # current = game.pac_loc #პაკმანის ლოკაცია
        # active_pills = game.get_active_pills_nodes() # საჭმელები
        # active_power_pills = game.get_active_power_pills_nodes() #გამაძლიერებლები
        # targets = active_pills + active_power_pills #ორივე ერთად

        # if game.get_eating_ghost() != -1:
        #     for i in game.ghost_locs:
        #         if i != 1292:
        #             targets.append(i) #adding ghosts outside of spawning box in the target list
        # print(game._graph)
#         nearest = game.get_target(current, targets, True, DM.PATH) #ვარჩევთ უახლოეს საჭმელს
#         best= game.get_best_dir_from(game.get_pacman_neighbors(),nearest,True,DM.PATH) #ვარჩევთ უფრო მომგებიან მიმართულებას საჭმელის სიახლოვის მიხედვით
#         self.pacman.set(best)
#

maybe neural algorithms?



#
#!/usr/bin/env python3
# from game.controllers import PacManControllerBase
# from game.pacman import Game, DM
#
# # from game.pac_gui import PacView
# import sys
# from os.path import dirname
#
# # hack for importing from parent package
# sys.path.append(dirname(dirname(dirname(__file__))))
# from search_templates import *
# from ucs import ucs
#
# # hint: class PacManProblem(Problem)...
# #       ... Ucs.search(problem)
#
#
# class MyAgent(PacManControllerBase):
#     def __init__(
#         self, human: bool = False, seed: int = 0, verbose: bool = False
#     ) -> None:
#         super().__init__(human, seed, verbose)
#
#         # You can initialize your own class variables here.
#
#     def tick(self, game: Game) -> None:
#         # Your implementation goes here.
#
#         # Dummy implementation: move in a random direction.
#         # You won't live long this way
#         directions = game.get_possible_pacman_dirs(False)
#         self.pacman.set(self.random.choice(directions))